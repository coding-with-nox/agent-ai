## Task
Fix compilation or runtime errors in {{stack.framework}} code.

{{task.description}}

## Error Output
{{task.error_context}}

## Previous Code
{{task.previous_attempt}}

## Requirements
- Identify the root cause of each error
- Fix all errors while preserving intended functionality
- Do not introduce new issues or regressions
- Keep the fix minimal — change only what is necessary
- Add comments explaining non-obvious fixes

## Output
Output the corrected file(s) wrapped in `<code filepath="...">` tags.
