## Task
Generate a service with its interface for {{stack.framework}}.

{{task.description}}

## Requirements
- Create an interface (I{ServiceName}) and implementation
- Use constructor injection for dependencies
- Follow {{stack.conventions}} architecture
- Include proper error handling and logging
- Use async/await with CancellationToken support
- Add XML doc comments on all public members

## Output
Generate the following files:
1. The service interface
2. The service implementation

Wrap each file in `<code filepath="...">` tags.
